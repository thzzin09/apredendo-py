import math
# ("import (nome da biblioteca)") pega toda a biblioteca
# ("from (nome da biblioteca) import ("oq vc quer dessa biblioteca") ")

num = float(input("digite um numero :"))
intera = math.floor(num)
print(f'o numero {num} tem a parte inteira = {intera}')
# floor arredonda para baixo